# Drone RL AirSim Requirements - 测试通过的稳定版本
# 基于实际成功运行环境：Windows 10 + AirSim 1.8.1 + CUDA 12.1 + Python 3.10

# =============================================================================
# 重要：精确版本 - 已完全测试通过
# =============================================================================

# 数值计算 - 使用NumPy 1.x避免兼容性问题
numpy==1.24.3

# 深度学习框架 - PyTorch CUDA 12.1 精确版本
--find-links https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121
torchaudio==2.1.2+cu121

# 计算机视觉 - 兼容NumPy 1.x的OpenCV版本
opencv-contrib-python==4.8.1.78

# AirSim通信库 - 解决encoding参数问题的版本
msgpack==0.6.2
msgpack-rpc-python==0.4.1

# AirSim仿真环境
airsim==1.8.1

# 强化学习库 - 稳定兼容版本
gymnasium==0.29.1
stable-baselines3==2.0.0

# 配置管理
omegaconf==2.3.0

# 数据分析 - 兼容NumPy 1.x
pandas==1.5.3

# 可视化工具
matplotlib==3.7.5
tensorboard

# 系统监控
psutil

# =============================================================================
# 可选依赖 - 按需安装
# =============================================================================

# 进度条和实用工具
tqdm
seaborn

# 科学计算
scipy

# =============================================================================
# 安装说明
# =============================================================================
# 1. 创建环境: conda create -n drone_rl python=3.10 -y
# 2. 激活环境: conda activate drone_rl  
# 3. 安装依赖: pip install -r requirements_windows_cuda121.txt
# 4. 测试环境: python -c "import torch; import cv2; import airsim; print('✅ 环境就绪!')"
# 5. 开始训练: python scripts/modern_train.py --total-timesteps 1000

# =============================================================================
# 已解决的问题
# =============================================================================
# ✅ 修复了msgpack encoding参数错误
# ✅ 修复了NumPy 2.x兼容性问题  
# ✅ 修复了Stable-Baselines3导入问题
# ✅ 确保AirSim 1.8.1完全兼容
# ✅ 支持CUDA 12.1和混合精度训练

# =============================================================================
# 可视化和日志记录
# =============================================================================
# TensorBoard 和日志
tensorboard>=2.12.0,<3.0.0
tensorboardX>=2.6,<3.0.0

# 绘图库
matplotlib>=3.6.0,<4.0.0
seaborn>=0.12.0,<1.0.0
plotly>=5.14.0,<6.0.0

# 进度条
tqdm>=4.65.0,<5.0.0

# 实验跟踪 (可选)
wandb>=0.15.0,<1.0.0

# =============================================================================
# 配置和序列化
# =============================================================================
# YAML 配置文件支持
PyYAML>=6.0,<7.0.0
omegaconf>=2.3.0,<3.0.0

# 对象序列化
cloudpickle>=2.2.0,<3.0.0
dill>=0.3.6,<1.0.0

# JSON处理
ujson>=5.7.0,<6.0.0

# =============================================================================
# 系统和网络
# =============================================================================
# 网络请求
requests>=2.28.0,<3.0.0
urllib3>=1.26.0,<3.0.0

# 系统监控
psutil>=5.9.0,<6.0.0

# 文件处理
h5py>=3.8.0,<4.0.0

# =============================================================================
# Windows 特定依赖
# =============================================================================
# Windows 路径处理
pathlib2>=2.3.7; sys_platform == "win32"

# Windows 事件循环 (异步处理)
pywin32>=306; sys_platform == "win32"

# =============================================================================
# 开发和测试工具
# =============================================================================
# 测试框架
pytest>=7.3.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0
pytest-xdist>=3.2.0,<4.0.0

# 代码格式化
black>=23.3.0,<24.0.0
isort>=5.12.0,<6.0.0

# 代码检查
flake8>=6.0.0,<7.0.0
mypy>=1.3.0,<2.0.0

# =============================================================================
# 性能和优化
# =============================================================================
# 数值优化
numba>=0.57.0,<1.0.0

# 内存优化
memory-profiler>=0.60.0,<1.0.0

# =============================================================================
# 可选：高级功能
# =============================================================================
# 超参数优化 (可选)
optuna>=3.2.0,<4.0.0

# 分布式训练 (可选)
ray[tune]>=2.4.0,<3.0.0

# Jupyter 支持 (开发时可选)
ipython>=8.12.0,<9.0.0
jupyter>=1.0.0,<2.0.0

# =============================================================================
# 注意事项
# =============================================================================
# 1. 此文件专为 Windows 10 + CUDA 12.1 环境优化
# 2. 某些包版本已锁定以确保兼容性
# 3. 建议在虚拟环境中安装
# 4. 安装顺序很重要，PyTorch 应该首先安装