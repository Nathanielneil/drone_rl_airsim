# AirSim坐标系统和单位说明

## **关键发现：速度单位错误导致无人机飞行异常**

### **问题原因**
1. **速度单位混乱**：配置文件中设置 `max_velocity: 300.0`，注释为"3米/秒 (300cm/s)"
2. **AirSim API**：`moveByVelocityAsync` 接受的参数单位是 **米/秒**，不是厘米/秒
3. **结果**：无人机实际最大速度变成 300米/秒（超音速！）

### **AirSim坐标系统**
```
AirSim使用NED坐标系 (North-East-Down):
├── X轴: 北方向为正
├── Y轴: 东方向为正  
└── Z轴: 下方向为正 (重要!)
```

### **速度单位**
- **位置**: UE4使用厘米 (cm)
- **速度**: AirSim API使用米/秒 (m/s)
- **时间**: 秒 (s)

### **修正对比**

#### **错误配置** (导致无人机异常飞行)
```yaml
max_velocity: 300.0  # 实际含义：300米/秒！
```

#### **正确配置**
```yaml
max_velocity: 3.0    # 实际含义：3米/秒
```

### **动作映射**
```python
# SAC输出: [-1, 1] 范围
# 速度计算: action * max_velocity
# 
# 错误情况：
# action = -0.5, max_velocity = 300.0
# vz = -0.5 * 300.0 = -150.0 米/秒 (向上)
# 
# 正确情况：
# action = -0.5, max_velocity = 3.0  
# vz = -0.5 * 3.0 = -1.5 米/秒 (向上)
```

### **Z轴方向说明**
- **正Z值 (+)**: 向下移动
- **负Z值 (-)**: 向上移动
- **高度控制**: 需要考虑NED坐标系

### **修正的配置参数**
```yaml
environment:
  # 速度设置 (米/秒)
  max_velocity: 3.0        # 原来：300.0 (错误)
  
  # 高度设置 (厘米，UE4单位)
  max_altitude: 300.0      # 3米 = 300厘米
  min_altitude: 40.0       # 0.4米 = 40厘米
  takeoff_height: 100.0    # 1米 = 100厘米
  
  # 目标设置 (厘米，UE4单位)
  goal_range:
    x: [-1000, 1000]      # ±10米 = ±1000厘米
    y: [-1000, 1000]      # ±10米 = ±1000厘米
    z: [40, 300]          # 0.4-3米 = 40-300厘米
```

### **训练脚本显示修正**
```python
# 修正前的显示 (误导性)
print(f"最大速度: {max_velocity} cm/s → {max_velocity/100} 米/秒")

# 修正后的显示 (正确)
print(f"最大速度: {max_velocity} 米/秒")
```

这个发现解释了为什么无人机会"一直垂直向上飞"！